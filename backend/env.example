DATABASE_URL=postgresql+asyncpg://smartnotes:smartnotes123@localhost:5432/smartnotes
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:3000"]

# Email Configuration (SMTP)
# Để gửi email thông báo khi đăng ký, cấu hình SMTP bên dưới

# ===== CÁCH 1: Email Server Riêng (Không cần App Password) =====
# Ví dụ với email server riêng (cPanel, DirectAdmin, v.v.):
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASSWORD=your-normal-password
# SMTP_FROM=Chí Tường Smart <noreply@yourdomain.com>
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false

# Hoặc nếu server dùng SSL trực tiếp (port 465):
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=465
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASSWORD=your-normal-password
# SMTP_FROM=Chí Tường Smart <noreply@yourdomain.com>
# SMTP_USE_TLS=false
# SMTP_USE_SSL=true

# ===== CÁCH 2: Gmail (Cần App Password) =====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password-16-ky-tu
# SMTP_FROM=Chí Tường Smart <your-email@gmail.com>
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false

# ===== CÁCH 3: Outlook/Hotmail =====
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASSWORD=your-password
# SMTP_FROM=Chí Tường Smart <your-email@outlook.com>
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false

# Nếu không cấu hình SMTP, đăng ký vẫn hoạt động nhưng không gửi email
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Bật tính năng gửi email nhắc nhở (true/false)
REMINDER_ENABLED=false

