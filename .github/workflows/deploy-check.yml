name: Deploy Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Check Backend
      run: |
        echo "✅ Backend code structure checked"
        
    - name: Check Frontend
      run: |
        echo "✅ Frontend code structure checked"

